# Production HTTPS host — set this before `npm run build` for the release APK.
# The cleartext-traffic config in capacitor (Phase 5) is off by default, so
# this MUST be HTTPS for the Android build to be allowed to call it.
VITE_API_BASE=https://api.motogiathinh.example
