<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security config for the guest kiosk app.

  Production: HTTPS only. The release APK should be built with
  VITE_API_BASE pointing at an https:// host.

  Development: cleartext is allowed for 127.0.0.1 and the 192.168.x.x /
  10.x.x.x private ranges so the debug APK can hit the local backend
  during LAN testing. Add specific subnets if your network uses a
  different range (e.g. 172.16.0.0/12).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.4.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
