# Dev mode — talks to the local backend on port 3001.
# Change the host to your LAN IP (e.g. http://192.168.1.42:3001) when testing
# from a phone on the same Wi-Fi.
VITE_API_BASE=http://127.0.0.1:3001
